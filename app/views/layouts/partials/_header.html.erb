<!--Navbar-->
<nav class="navbar navbar-expand-lg fixed-top bg-white py-3">
  <div class="container d-flex justify-content-between align-items-center">
    <!-- Logo -->
    <a href="#" class="navbar-brand">
      <%= image_tag "https://dsazhl88swmg2.cloudfront.net/helpalife/helpalifelogo.png", class: "navbar-brand", style: "height:55px;" %>
    </a>

    <!-- Profile Icon + Toggler for Mobile -->
    <div class="d-lg-none d-flex align-items-center gap-3">
      <!-- Profile Icon (SVG) -->
      <a href="profile.html" class="text-decoration-none">
        <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="currentColor"
             class="bi bi-person-circle text-secondary" viewBox="0 0 16 16">
          <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0"/>
          <path fill-rule="evenodd"
                d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8m8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1"/>
        </svg>
      </a>

      <!-- Toggler Button -->
      <button class="navbar-toggler border-0" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>

    <!-- Desktop Navbar -->
    <div class="collapse navbar-collapse d-none d-lg-flex justify-content-center" id="navbarNav">
      <ul class="navbar-nav gap-4">
        <li class="nav-item">
          <a href="<%= root_path %>" class="nav-link fw-semibold<%= ' text-danger' if current_page?(root_path) %>">Home</a>
        </li>
        <li class="nav-item">
          <a href="<%= about_path %>" class="nav-link fw-semibold<%= ' text-danger' if current_page?(about_path) %>">About us</a>
        </li>
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle fw-semibold<%= ' text-danger' if current_page?(donors_path) || request.path.include?('Donate Now') %>" id="emergencyDropdown" role="button" data-bs-toggle="dropdown">
            Emergency
          </a>
          <ul class="dropdown-menu shadow-sm rounded-3 border border-light">
            <a href="<%= donors_path(anchor: 'find-donors') %>" class="dropdown-item fw-semibold py-2<%= ' text-danger' if current_page?(donors_path) %>">Find Donors</a>
            <li><a class="dropdown-item fw-semibold py-2<%= ' text-danger' if request.path.include?('Donate Now') %>" href="#">Donate Now</a></li>
          </ul>
        </li>
        <li class="nav-item">
          <a href="<%= donations_path %>" class="nav-link fw-semibold<%= ' text-danger' if current_page?(donations_path) %>">Donations</a>
        </li>
        <li class="nav-item">
          <a href="<%= register_path %>" class="nav-link fw-semibold<%= ' text-danger' if current_page?(register_path) %>">Register</a>
        </li>
      </ul>
    </div>

    <!-- Desktop Buttons -->
    <div class="navbar-buttons d-none d-lg-flex gap-3">
      <a href="#">
        <button type="button" class="btn btn-outline-danger px-3 py-2 fw-semibold">
          Login
        </button>
      </a>
      <a href="Signup.html">
        <button type="button" class="btn btn-danger px-3 py-2 fw-semibold">
          Sign up
        </button>
      </a>
      <a href="profile.html">
        <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="currentColor" class="bi bi-person-circle mt-2 text-secondary" viewBox="0 0 16 16">
          <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0"/>
          <path fill-rule="evenodd" d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8m8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1"/>
        </svg>
      </a>
    </div>
  </div>

  <!-- Mobile Offcanvas Menu -->
  <div class="offcanvas offcanvas-end d-lg-none" tabindex="-1" id="offcanvasNavbar">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title">Menu</h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body d-flex flex-column justify-content-between align-items-start">
      <ul class="navbar-nav text-start">
        <li class="nav-item">
          <a href="<%= root_path %>" class="nav-link fw-semibold mb-4<%= ' text-danger' if current_page?(root_path) %>">Home</a>
        </li>
        <li class="nav-item">
          <a href="<%= about_path %>" class="nav-link fw-semibold mb-4<%= ' text-danger' if current_page?(about_path) %>">About us</a>
        </li>
        <li class="nav-item dropdown w-100">
          <a href="#" class="nav-link dropdown-toggle fw-semibold mb-4 w-100 text-start<%= ' text-danger' if current_page?(donors_path) || request.path.include?('Donate Now') %>" id="emergencyDropdownMobile" role="button" data-bs-toggle="dropdown">
            Emergency
          </a>
          <ul class="dropdown-menu shadow-sm rounded-3 border border-light w-100" aria-labelledby="emergencyDropdownMobile">
            <li><a class="dropdown-item fw-semibold py-2<%= ' text-danger' if current_page?(donors_path) %>" href="<%= donors_path(anchor: 'find-donors') %>">Find Donors</a></li>
            <li><a class="dropdown-item fw-semibold py-2<%= ' text-danger' if request.path.include?('Donate Now') %>" href="#">Donate Now</a></li>
          </ul>
        </li>

        <li class="nav-item">
          <a href="<%= donations_path %>" class="nav-link fw-semibold mb-4<%= ' text-danger' if current_page?(donations_path) %>">Donations</a>
        </li>
        <li class="nav-item">
          <a href="<%= register_path %>" class="nav-link fw-semibold<%= ' text-danger' if current_page?(register_path) %>">Register</a>
        </li>
      </ul>
      <!-- Mobile Login/Signup Buttons -->
      <div class="offcanvas-footer p-3 w-100">
        <div class="navbar-buttons d-flex flex-column gap-3">
          <a href="#">
            <button type="button" class="btn btn-outline-danger px-3 py-2 fw-semibold w-100">
              Login
            </button>
          </a>
          <a href="Signup.html">
            <button type="button" class="btn btn-danger px-3 py-2 fw-semibold w-100">
              Sign up
            </button>
          </a>
        </div>
      </div>
    </div>
  </div>
</nav>

<!--Navbar-->